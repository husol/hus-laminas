<div class="modal-dialog modal-lg" role="document">
  <!--Content-->
  <div class="modal-content">
    <!--Header-->
    <div class="modal-header bg-blue text-white">
      <h3><?=(isset($this->myUser)) ? "Update" : "New"?> User</h3>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>

    <!--Body-->
    <div class="modal-body mb-0">
      <form id="formUser" class="eventEnterKey" data-enterkey="btnSave" novalidate>
        <div class="row">
          <div class="form-group label-float col-md-4">
            <label for="fullName" class="required">Họ và tên</label>
            <input type="text" class="form-control" id="fullName" name="fullName" value="<?=isset($this->myUser) ? $this->myUser->full_name : ''?>" required>
          </div>
          <div class="form-group label-float col-md-4">
            <label for="role">Vai trò</label>
            <select class="form-control" id="role" name="role">
              <option value="0"<?=isset($this->myUser) && $this->myUser->role == 0 ? ' selected' : ''?>>CLIENT</option>
              <option value="1"<?=isset($this->myUser) && $this->myUser->role == 1 ? ' selected' : ''?>>STAFF</option>
              <option value="2"<?=isset($this->myUser) && $this->myUser->status == 2 ? ' selected' : ''?>>ADMIN</option>
            </select>
          </div>
          <div class="form-group label-float col-md-4">
            <label for="status">Trạng thái</label>
            <select class="form-control" id="status" name="status">
              <option value="0"<?=isset($this->myUser) && $this->myUser->status == 0 ? ' selected' : ''?>>INACTIVE</option>
              <option value="1"<?=isset($this->myUser) && $this->myUser->status == 1 ? ' selected' : ''?>>ACTIVE</option>
            </select>
          </div>
        </div>
        <div class="row">
          <div class="form-group label-float col-md-4 label-float">
            <label for="email" class="required">Email</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="email@example.com" value="<?=isset($this->myUser) ? $this->myUser->email : ''?>" required>
          </div>
          <div class="form-group label-float col-md-4">
            <label for="password"<?=isset($this->myUser) ? '' : ' class="required"'?>>Mật khẩu</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="<?=isset($this->myUser) ? 'Bỏ trống để giữ mật khẩu cũ' : 'Mật khẩu'?>"<?=isset($this->myUser) ? '' : ' required'?>>
          </div>
          <div class="form-group label-float col-md-4">
            <label for="confirmPassword">Nhập lại mật khẩu</label>
            <input type="password" class="form-control" id="confirmPassword" name="confirmPassword">
          </div>
        </div>
        <div class="row">
          <div class="form-group label-float col-md-4">
            <label for="mobile">Số điện thoại</label>
            <input type="text" class="form-control" id="mobile" name="mobile" value="<?=isset($this->myUser) ? $this->myUser->mobile : ''?>">
          </div>
          <div class="form-group label-float col-md-8">
            <label for="address">Địa chỉ</label>
            <input type="text" class="form-control" id="address" name="address" value="<?=isset($this->myUser) ? $this->myUser->address : ''?>">
          </div>
        </div>

        <input type="hidden" id="idRecord" name="idRecord" value="<?=isset($this->myUser)? $this->myUser->id : 0?>">
      </form>
    </div>

    <div class="modal-footer justify-content-center">
      <button id="btnSave" class="btn btn-primary"><span class="btn-label"><i class="fa fa-save"></i></span> <?=$this->layout()->translator->getLang('btn_save')?></button>
    </div>
  </div>
  <!--/.Content-->
</div>
